<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" type="text/css">
        <title>Job History</title>
    </head>
    <body>
        <div class="container">

            {% if jobs is not empty %}
                <h2>Jobs Entered So Far</h2>
                <h4>
                {% for job in jobs %}
                    {{ job.getWhenEmployed }} /
                    {{ job.getJobTitle }} /
                    {{ job.getEmployer }} <br>
                {% endfor %}
                </h4>
                <br>
            {% endif %}

            <h2>Please enter a job to add to your employment history:</h2>
            <form action="/" method="post">
                <div class="form-group">
                    <label for="when_employed">When were you employed?</label>
                    <input id="when_employed" class="form-control" name="when_employed" type="text">
                </div>
                <div class="form-group">
                    <label for="job_title">Title</label>
                    <input id="job_title" class="form-control" name="job_title" type="text">
                </div>
                <div class="form-group">
                    <label for="employer">Who was your employer?</label>
                    <input id="employer" class="form-control" name="employer" type="text">
                </div>
                <button class="btn" type="submit" name="add_new_job_button_clicked">Add a Job!</button>
            </form>
            <br>
            <form action="/" method="post">
                <button class="btn btn-warning" type="submit" name="delete_all_jobs_button_clicked">Delete All Jobs</button>
            </form>
        </div>
    </body>
</html>
